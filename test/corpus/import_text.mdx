=============
Single import
=============
import Button from '../components/Button.astro';

---

(document
  (jsx_section
    (import_statement
      (import_start)
      (identifier)
      (string
        (single_quoted_string))
      (semicolon))
    (end_of_line)))
