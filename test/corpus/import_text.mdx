=============
Single import
=============
import Button from '../components/Button.astro';

---

(document
  (jsx_section
    (import_statement
      (import_start)
      (jsx_identifier)
      (string
        (single_quoted_string))
      (semicolon))
    (end_of_line)))

=============
Import within markdown paragraph
============

import Button from '../components/';
