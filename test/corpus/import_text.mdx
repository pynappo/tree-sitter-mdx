=============
Single import
=============
import Button from '../components/Button.astro';

---

(document
  (jsx_section
    (import_statement
      (import_start)
      (jsx_identifier)
      (jsx_string)
      (semicolon))
    (new_line))
  (new_line
    (eof)))
