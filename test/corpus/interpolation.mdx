=====
Zero-width interpolation
=====
{}

----

(document
  (markdown_section
    (interpolation
      (interpolation_start)
      (interpolation_end))
    (new_line))
  (new_line
    (eof)))

=====
Interpolation with empty body
=====
{  	  }

----

(document
  (markdown_section
    (interpolation
      (interpolation_start)
      (interpolation_content)
      (interpolation_end))
    (new_line))
  (new_line
    (eof)))

=====
Interpolation
=====
{asdf} asdfasdfasdf
{asdf}

----

(document
  (markdown_section
    (interpolation
      (interpolation_start)
      (interpolation_content)
      (interpolation_end))
    (markdown_word)
    (new_line)
    (interpolation
      (interpolation_start)
      (interpolation_content)
      (interpolation_end))
    (new_line))
  (new_line
    (eof)))

=====
Escaped slash
=====
this should be escaped\\asdfasdf


----

(document
  (markdown_section
    (markdown_word)
    (escaped_char
      (escape))
    (markdown_word)
    (new_line))
  (new_line))
